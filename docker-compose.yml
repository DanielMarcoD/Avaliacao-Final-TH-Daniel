version: '3.8'
services:
  web:
    build: .
    container_name: enhanced-web-scanner
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./reports:/app/reports
      - ./scanner_db.sqlite:/app/scanner_db.sqlite
    restart: unless-stopped
